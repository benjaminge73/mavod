# Production runtime dependencies (mavod-bot container).
# Locked versions — bump intentionally via pip-compile or manual edit.

# HTTP / network
httpx==0.28.1
requests==2.34.2
certifi==2025.8.3
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0

# Telegram
python-telegram-bot[job-queue]==22.8

# Torrent parsing + indexer XML responses
bencodepy==0.9.5
beautifulsoup4==4.15.0
lxml==6.1.1

# Misc
python-dotenv==1.2.2
tenacity==9.1.4
